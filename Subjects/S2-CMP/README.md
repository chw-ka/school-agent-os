# S2-CMP

中二電腦科（CMP）工作區。

## 建議結構

- `past-papers/`：歷屆試卷（`{YYYY-YYYY}/Term {01|02}/` 下分 WrittenExam、PracticalAssessment、PracticalMock）
- `notes/`：教學筆記、worksheet、課堂專題（例如 `01_MobileAppDevelopment/`）
- `exam-input/`、`source/`：可按需要新增（參考 `S3-CMP/`）
