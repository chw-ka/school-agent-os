# S3-CMP

中三電腦科（CMP）工作區。

建議放法：
- `exam-input/`：題目 JSON（Markdown 內容）
- `generated/`：工具輸出（docx/pdf）
- `notes/`：規格、題型標準、marking scheme（不含個資）

## S3-CMP
中三 CMP 教學與出卷工作區。

建議結構：
- `source/`: 原始題庫/考古題（Markdown / JSON）
- `output/`: 自動產出的試卷（DOCX）
- `notes/`: 教師備註與出題規格（可選）

